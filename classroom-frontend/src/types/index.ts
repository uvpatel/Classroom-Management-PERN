export type Subject = {
    id: number;
    name: string;
    code: string;
    description: string;
    department: string;
    createdAt: Date;
}

export type Subjects = {
  id: string;
  code: string;
  name: string;
  department: string;
  description: string;
};