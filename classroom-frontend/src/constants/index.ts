export const DEPARTMENTS = [
  'Mathematics',
  'Science',
  'English',
  'History',
  'Geography',
]

export const DEPARTMENT_OPTIONS = DEPARTMENTS.map((dept) => ({
    value: dept,
    lable: dept,
}))
